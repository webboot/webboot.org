export const View = state => div([
  h3('canary notice:'),
  p(b('we will not and have never shared any data about our users with anyone.')),

  p([
    ' if this url disappears,',
    ' it means that the rule above has been broken by force.',
  ]),

  p('TODO: get canary comic'),
])
